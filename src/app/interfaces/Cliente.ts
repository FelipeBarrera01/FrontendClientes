import { NodeCompatibleEventEmitter } from 'rxjs/internal/observable/fromEvent';

export interface Clientes{
    id?:number;
    nombre:string;
    apellido:string;
    createAt:string;
    email:string;

}